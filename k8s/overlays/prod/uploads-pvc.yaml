apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: burty-uploads
spec:
  accessModes: [ReadWriteMany]
  # EFS(efs-sc) 또는 그에 준하는 RWX StorageClass 를 지정할 것.
  # RWO 만 있는 클러스터라면 replicas 를 1로 낮추거나 S3 어댑터로 전환해야 한다.
  storageClassName: efs-sc
  resources:
    requests:
      storage: 20Gi
